# EasyPhotoBackup

Direct automatic file back up from your Android device to your computer over your local Wi-Fi network.

No cloud storage, no third-party servers, and no subscription required.  
(also no ads, no tracking, no spying on you, you own your data from the beginning to the end)

## How It Works

1. Install the host app on your PC.
1. Pair your phone app with the host app.
1. Set up what you want to back up.
1. Every time your phone is in the same Wi-Fi network with your PC, and the PC is on, your files will be backed up to the PC in the background.

// Add a diagram here with a phone, Wi-Fi router and a PC

All backup data is end-to-end encrypted, including the file metadata. The information about the used protocols can be found at the bottom of the page.

The file transfer can be safely interrupted and resumed at any point (including mid file), the used storage is crash-proof (thanks to LMDB as the local storage).

## Caution: The Application Is in Development

Even though most of the groundwork is already done (and I already use it for backing up my files), there are many features to add before the app can be considered useful to other people.

What is left until the first beta:

- UI for selecting folders to back up (right now it automatically backs up a selection of folders)
- UI for pairing with MITM protection (6 digit SAS codes)
- Activity log
- Fix issues related to background task lifetime
- UI for configuring the host application (PC app)

Before the first beta, neither protocol nor local storage versions are fixed and updating the app may require completely reinstalling it with removing all the configs and storage.

What is left until calling it functional enough for version 1.0:

- Per-host and per-folder backup settings
- The ability to set fixed IP (and with that the opt-in ability to work through internet)
- UI prettifying
- Giving it a name
- Add fuzz-testing
- Make builds reproducible

Version 1.0 is an imaginary milestone for calling the app ready for publishing. The app should already be functional (and stable) from the first beta (from the moment of giving it any version number).

## Building Requirements

Windows/Linux/Mac:

- git
- CMake 3.22.1 or higher
- a compiler supporting C++20

Android:
- Android Studio with NDK installed (SDK versions may change, you'd need to check the latest config)

## Building

Linux/Mac:
- cd build
- cmake ..
- make

Windows with MSVC
- cd build
- cmake ..
- Open the generated project in VS or use MSVC toolchain to build

## Stance on AI

This is year 2026, many developers (and non-developers) use agentic AI for application development, however the quality and reliability of the code generated by these agentic AI varies a lot. C++ code is a particularly dangerous place, and even though humans are also bad at writing safe C++ code, we at least have the principle of code ownership.

The project follows these guidelines that apply for any contributions including myself (see examples below):
- No C++ LLM-generated (or AI-generated in general) code can be submitted to the repository.
  - One small exception is adding new tests to already existing test suites. Editing existing tests and adding completely new tests (e.g. for new functionality) should still be done only by human contributors without using AI-generated code.
- Kotlin code still must be mostly authored or edited by humans. Using LLMs is permitted while done in moderation and thoroughly reviewed by a human before bringing it to the repository (both merge requests or submitting directly).
- XML layouts are permitted to be created or edited by LLM, but should be reviewed by humans.
- All other code and configs may only be authored by humans.
- It is permitted to use any types of AI to analyze the code, generate reports and suggestions. Automated creation of Issues is also permitted in moderation, but not merge requests (Pull Requests).

Examples to demonstrate the difference:
- There is a function that is covered by unit tests. It is OK to use LLM to add new tests for the function to the same test suite.
- There is a function that is not covered by unit tests. It is not OK to use LLM to add tests to this function, they should be authored manually.
- A contributor authored a new UI functionality in Kotlin using LLM-driven code completion for assistance, or used agentic AI to research the existing logic and suggest solutions (but no code was generated by the agentic AI). This feature is permitted to be added.
- Agentic AI was used to add a new UI functionality, prompted by a contributor. This functionality is not allowed to be added.

## Used Protocols

Noise_XX_25519_ChaChaPoly_BLAKE2b is used for exchanging pairing information, and its handshake hash used to derive short authentication string (SAS) to confirm the pairing.

Static keys for Noise protocol are generated per unique pairing, and re-generated even if the same host and client application are re-paired.

Noise_KK_25519_ChaChaPoly_BLAKE2b is used for file exchange, however accompanied with sending a connection ID in plain-text (static ID generated based on public keys of this device pairing).

Both Noise implementation is covered with tests, including tests comparing it with Rust implementation from [noiseexplorer.com](https://noiseexplorer.com))

Used primitives (by [monocypher](https://monocypher.org/manual/aead):
- X25519 for public key cryptography
- BLAKE2b for hashing
- XChaCha20/Poly1305 for authenticated symmetrical encryption

A custom UDP protocol similar to Bonjour protocol (but much simpler and more strict) is used for network service discovery (NSD).

The file transfer is split in equally sized chunks with equally sized number of chunks between answers, both padded, which allows hiding the number of files and their sizes being transmitted.

All the protocols ensure the sizes of the messages when possible, or otherwise set maximum allowed sizes to variable sized data passed over network, rejecting anything that doesn't fit.

### What Is Protected and What Is Not

- An attacker trying to perform a man-in-the-middle (MITM) attack during device pairing process needs to correctly forge keys for a handshake to produce the same 6-digit SAS on both sides. This is without knowing in advance the keys of the other party (which are regenerated for each pairing attempt). This makes the MITM attack highly unlikely.
- An attacker connected to the same network as the client can determine that the device is using this application (by seeing the NSD requests).
- By the design of the protocol, an attacker observing the network during the file exchange communication is not able to determine the content of the data. Trying to tamper with the data in flight or repeating the data will result in authentication failure and terminate the transfer.
- An attacker, who has previously been on the same network with the host and probed the host, or saw the client and the host exchanging information, can then create an application in other Wi-Fi networks to identify the client by answering the NSD requests.
- An attacker who is on the same Wi-Fi network with the host and the client during the file exchange can roughly estimate the total size of all the files exchanged (sum of the sizes, but not individual sizes). However, chunk padding, potential resume of partial transfer, and metadata of individual files, all complicate the estimation.
- The protocol is designed so that an attacker cannot forge a host application that can trick the client to exchange the files with it. At most, first chunks of encrypted data will be sent which could not be decrypted, but the connection will be closed as soon as the host is required to answer the status.
- By the design of the protocol, an attacker cannot forge a client application that would allow uploading files to the host, the connection will be dropped as soon as the first file transfer is attempted after the failed handshake.
